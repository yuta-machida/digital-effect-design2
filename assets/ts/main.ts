import * as variable from "./variable";
import { bgImageAnimation } from "./animation/reductionAnimation";

document.addEventListener("DOMContentLoaded", () => {
  bgImageAnimation(variable.img_main_bottle);
});
